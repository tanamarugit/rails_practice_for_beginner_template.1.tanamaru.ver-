<!DOCTYPE html>
<html>
  <head>
    <title>RailsPractice</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |message_type, message| %>
      <%= message %>
    <% end %>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand">QuestionApp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a class="nav-link" href="/questions">質問一覧</a>
          </li>
        <li>
          <% if current_user %>
            <a class="nav-link" href="/users">ユーザー一覧</a>
          <% end %>
        </li>
          <li class="nav-item">
            <% if current_user == nil %>
              <%= link_to  "ログイン", login_path, class: 'nav-link'%>
            <% else %>
              <%= link_to  "ログアウト", logout_path, method: :delete, class: 'nav-link'%>
            <% end %>
          </li>
          <li class="nav-item">
            <% if current_user %>
              <%= link_to  "質問投稿", new_question_path, class: 'nav-link'%>
              <%= "ログイン中のユーザーは#{current_user.name}さんです" %>
            <% else %>
              <%= link_to  "ユーザー登録", new_user_path, class: 'nav-link' %>
            <% end %>
          </li>
        </ul>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
